# VICI Tech Assignment — LLM Gateway + Browser Automation Agent
This document is the authoritative spec for this repository. It defines architecture, coding standards, safety boundaries, testing rules, and execution constraints. Follow this strictly.

Scope & Precedence
- Applies to the entire repository unless a more deeply nested `AGENTS.md` overrides a part of it.
- Direct user instructions can override specific items when explicitly requested.
- Keep code modular; do not add features beyond scope.

---

## 0. Mission

Build a production-style system that combines:
- LLM Gateway (LiteLLM-style, OpenAI-compatible)
- Browser Automation Agent (Playwright)

The system must be runnable end-to-end, produce verifiable artifacts, include structured logs, enforce safety boundaries, be modular and extensible, and pass tests at every stage.

Do NOT skip structure. Do NOT hardcode hacks. Do NOT add features beyond scope.

---

## 1. Architecture

Frontend (React + TSX)
        ↓
FastAPI Backend
   ├── LLM Gateway
   └── Agent Runner
           ↓
    Playwright Agent
           ↓
Artifacts + Logs

---

## 2. Technology Stack (MANDATORY)

- Backend: Python 3.11+, FastAPI, Pydantic, Uvicorn, Pytest, JSON structured logging
- Agent: Playwright (Python), HTML→PDF via Playwright print or python-pptx, deterministic dry-run
- Frontend: React + TypeScript (TSX), clean layout, minimal CSS
- Containerization: Docker, docker-compose, Makefile

---

## 3. Repository Layout (REQUIRED)

- backend/app: FastAPI app, routers, providers, middleware, models
- backend/tests: pytest tests for gateway and agent runner
- agent.py: Standalone agent CLI and workflow
- frontend/: React app (Vite-style) with required components
- runs/: Per-run artifacts served by backend
- outputs/ and run_logs/: Generated by agent inside per-run directories

---

## 4. LLM Gateway

### 4.1 Endpoints
- GET /health → returns status, version, timestamp
- POST /v1/chat/completions → OpenAI-compatible minimal schema
  - Generate request_id (UUID)
  - Measure latency
  - Include provider metadata
  - Return usage structure (even if mocked)

### 4.2 Provider Abstraction
- Interface: generate(messages, temperature, max_tokens) → text, usage, meta
- Providers: claude_cli.py, openai.py, mock.py (all implement the same interface)
- Router may NOT call providers directly; route through abstraction layer

### 4.3 Observability
- Log every request as JSON lines including: request_id, route, provider, model, latency_ms, retry_count, error (if any)

### 4.4 Safety Boundaries
- Request size limit
- Timeout per request
- Concurrency limit
- Provider allowlist
- Model allowlist
- Retry limit (max 2 retries)
- Never allow unlimited retries or unbounded input
- Safeguardrails guard layer (see `safety/`): prompt + response scanning, redaction, and optional JSON/YAML policies via `GATEWAY_SAFEGUARD_*` / `AGENT_SAFEGUARD_*` env vars with audit logs.

---

## 5. Agent

### 5.1 CLI
- Run: python agent.py --ticker AAPL --source URL --gateway URL

### 5.2 Workflow
1. Launch browser (headless)
2. Navigate to source URL, or Yahoo TW flow:
   - Yahoo TW flow opens `https://tw.stock.yahoo.com/`, searches for the ticker, navigates to its quote page
3. Extract main content
4. Save screenshots
5. Save trace.zip
6. Call Gateway endpoint
7. Generate: report.md and slides.pdf
8. Save structured run.json

### 5.3 Logs
- run_logs must include: run.json, trace.zip, screenshots/, request_ids used
- run.json must include: ticker, source, steps, timings, artifacts, model used, latency summary

### 5.4 Responsibilities and Outputs
- News Agent (`agents/news_agent.py`)
  - Encapsulated `NewsAgent` class handles both collection (Yahoo/Google scrape, deterministic dry-run) and micro-summaries via the gateway; persists raw list to `run_logs/news.json` and outputs `sections.news`/`sections.news_micro` exclusively.
  - Event enrichment logic also lives inside the agent so `agent.py` only orchestrates results instead of duplicating parsing heuristics.
- Finance/Trader Agents (`agents/finance_agent.py`, `agents/trader_agent.py`)
  - `FinanceAgent` owns every LLM prompt related to finance/trader theses (sentiment, overview bullets, watchlist, KPI inference) while `trader_agent.py` continues to compute deterministic technical signals.
  - These agents only consume gateway responses; Yahoo data fetching was removed to keep the responsibilities pure.
- YFinance Agent (`agents/yfinance_agent.py`)
  - `YFinanceAgent` is the single interface for Yahoo Finance data: snapshot KPIs/analysis, intraday price stats, sparkline series, and Chinese company-name localization (via Yahoo quote HTML when APIs return English strings).
  - Provides deterministic dry-run fallbacks by returning empty structures when Yahoo endpoints are unreachable.

---

## 6. Artifacts

outputs/
- report.md (finance/trader‑first structure; must include Source URL, LLM summary, Timestamp)
  - Emphasize financial conclusions and trader insights; any news summary appears at the end if included.
  - In Yahoo mode, Source URL reflects the visited quote page.
- slides.pdf (research summary, highlights, risks section)
- checksums.txt (sha256 of artifacts)
- run.json includes `safety_events` for every sanitized LLM call/artifact so the frontend enforces transparency.

---

## 7. Frontend

### 7.1 Layout
- Left panel: ticker, source URL, model selector, run button
- Right panel tabs: Report, News, Logs, History

### 7.2 Components (REQUIRED)
- Layout.tsx, RunForm.tsx, RunStatus.tsx, ArtifactViewer.tsx (Report view), NewsViewer.tsx, LogViewer.tsx

### 7.3 Behavior
- Trigger backend run
- Poll run status
- Report tab renders finance/trader content only from `sections.fin_analysis`, `sections.trader_signals`, `sections.kpis`, `sections.finance_basic`, `sections.watch_items`.
- News tab renders news‑only content from `sections.news` and `sections.news_micro` with sentiment/source/keywords; must not repeat KPI/valuation/Trader signals.
- Logs tab displays gateway/LLM calls and browser console logs.
- History tab lists prior runs with quick links to artifacts.

### 7.4 Content Boundaries (No Duplication)
- Report tab must NOT display per‑news lists, event lists, or overall news sentiment; it focuses on finance/trader metrics and watch items.
- News tab must NOT display KPI/valuation cards or trader indicators; it focuses on sentiment breakdown, source categories, keywords, and per‑news summaries.
- This separation is enforced in components and considered acceptance‑critical.

---

## 8. Testing Rules (pytest)

### Gateway
- /health works
- /v1/chat/completions schema valid
- Retry logic works
- Timeout behavior works
 - Notes: mock provider supports input triggers for tests (e.g., `FAIL_ONCE`, `TIMEOUT`, `LEAK_SECRET`).

### Agent
- Dry-run mode
- report.md created
- run.json created
 - Sections integrity: `news`/`news_micro` present for News; `fin_analysis`/`trader_signals` present for Report.

---

## 9. Docker & Makefile

Makefile targets:
- make install, make test, make run-gateway, make run-agent-demo, make run-frontend, make docker-up, make docker-down

docker-compose must launch:
- backend
- frontend

---

## 10. Coding Standards

- No global variables; use dependency injection where appropriate
- No hardcoded API keys; config via env variables
- Pydantic models for API schemas
- Structured JSON logging for observability
- Type hints everywhere

---

## 11. Execution Rules

Order of work:
1) Backend skeleton → 2) Add tests → 3) Keep gateway working → 4) Build agent → 5) Build frontend

Definition of Done per phase:
- Code runs without error; Tests pass; Logs generated; Clear output files exist

---

## 12. Performance Mindset

- Minimize latency; avoid blocking calls; keep code modular
- Ensure reproducibility and deterministic dry-run mode
- Provide full traceability

---

## 13. Prohibited

- Giant monolithic files; skipping abstraction layer; mixing UI and backend logic
- Unbounded input handling; silent failures; hidden side effects

---

## 14. Final Goal

Deliver an engineering-grade LLM Gateway + Browser Agent system demonstrating:
- Provider abstraction, Observability, Safety boundaries, Verifiable artifacts, Reproducible runs, Full-stack integration

---

## 15. Environment Variables (Reference)

- GATEWAY_VERSION
- GATEWAY_REQUEST_TIMEOUT_S
- GATEWAY_MAX_RETRIES (capped at 2)
- GATEWAY_MAX_INPUT_CHARS
- GATEWAY_MAX_CONCURRENCY
- GATEWAY_ALLOWED_PROVIDERS (e.g., mock,openai,claude)
- GATEWAY_ALLOWED_MODELS (e.g., mock-01,gpt-3.5-turbo,claude-3-haiku)
- AGENT_TIMEOUT_S
- AGENT_MODEL

---

## 16. Acceptance Checklist

- [ ] Gateway endpoints implemented and pass tests
- [ ] Provider abstraction with mock/openai/claude
- [ ] JSON logs with required fields
- [ ] Safety boundaries enforced
- [ ] Agent CLI produces report.md, slides.pdf, run.json, screenshots, trace.zip
- [ ] Frontend triggers run, polls status, shows artifacts and logs
- [ ] Docker + Makefile targets work
 - [ ] Report and News tabs have no duplicated content (finance/trader vs news separation)
